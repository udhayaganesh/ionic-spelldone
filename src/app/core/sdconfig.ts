//let backendHost = location.host.indexOf('localhost') == 0 ? 'http://localhost:8080/api' : 'https://spelldoneapi.appspot.com/api';
// let backendHost = location.host.indexOf('localhost') == 0 ? 'http://localhost:8080/api' : 'https://spelldoneapi-test.appspot.com/api';

let backendHost = 'https://spelldoneapi-test.appspot.com/api';
export let sdconfig = {
  backendHost: backendHost,
  baseResourceUrl: 'assets/data/',
}
