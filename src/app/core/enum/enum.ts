export enum SessionType {
  PRACTICE = 'practice',
  TEST = 'test'
}

export enum Languages {
  ENGLISH = 'English'
}

export enum PlanType {
  REGIONAL = 'Regional',
  NATIONAL = 'National'
}

export enum Role {
  ADMIN = 'admin'
}

export enum Price {
  REGIONAL = 60,
  NATIONAL = 80
}

export enum OauthProvider {
  GOOGLE = 'GOOGLE',
  FACEBOOK = 'FACEBOOK'
}